services:
  - type: web
    name: expert-plu
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --preload --access-logfile - --error-logfile - --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
